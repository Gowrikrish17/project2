docker ps
docker run -dit containerid
docker ps | grep containerid
docker ps -a
docker exec -it containerid /bin/bash
docker ps

vi Dockerfile
docker build . -t imgid buil1.0
docker run --name imgid -d -p 8087:80 imgid

kubectl get pods
kubectl get svc
kubectl apply -f docker-compose.yml

          
